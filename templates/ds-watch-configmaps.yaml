{{- $ds := index .Values.ressuite "ds-watch-be" -}}
{{- if $ds.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ tpl (index $ds.existingConfigMaps "logs") . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "chart-wrapper.dsAppLabel" . }}
    {{- include "chart-wrapper.labels" . | nindent 4 }}
data:
  {{ (.Files.Glob "files/ds-watch/internal-config/logs/*").AsConfig | nindent 2 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ tpl (index $ds.existingConfigMaps "db") . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "chart-wrapper.dsAppLabel" . }}
    {{- include "chart-wrapper.labels" . | nindent 4 }}
data:
  {{- $glob := "files/ds-watch/internal-config/db/*" -}}
  {{- $internal := ((.Files.Glob $glob).AsConfig | fromYaml) | default dict -}}
  {{- $externalRaw := (($ds.internalConfig | default dict).dbExternal | default dict) -}}
  {{- $external := dict -}}
  {{- range $k, $v := $externalRaw }}
  {{- if kindIs "string" $v }}
  {{- $path := trimPrefix "./" ($v | replace "\\" "/") -}}
  {{- $_ := set $external $k (required (printf "missing file for ressuite.ds-watch-be.internalConfig.dbExternal.%s: %s" $k $path) ($.Files.Get $path)) -}}
  {{- else }}
  {{- $_ := set $external $k $v -}}
  {{- end }}
  {{- end }}
  {{- mergeOverwrite $internal $external | toYaml | nindent 2 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ tpl (index $ds.existingConfigMaps "search") . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "chart-wrapper.dsAppLabel" . }}
    {{- include "chart-wrapper.labels" . | nindent 4 }}
data:
  {{ (.Files.Glob "files/ds-watch/internal-config/search/*").AsConfig | nindent 2 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ tpl (index $ds.existingConfigMaps "templateExcel") . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "chart-wrapper.dsAppLabel" . }}
    {{- include "chart-wrapper.labels" . | nindent 4 }}
binaryData:
  {{ (.Files.Glob "files/ds-watch/internal-config/templateExcel/*").AsSecrets | nindent 2 }}
{{- end }}
