{{- $onlineWatch := (get (.Values.ressuite | default dict) "online-watch-be") | default dict -}}
{{- if and $onlineWatch $onlineWatch.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ tpl (index $onlineWatch.existingConfigMaps "logs") . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "chart-wrapper.onlineWatchAppLabel" . }}
    {{- include "chart-wrapper.labels" . | nindent 4 }}
data:
  {{ (.Files.Glob "files/onlinewatch/internal-config/logs/*").AsConfig | nindent 2 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ tpl (index $onlineWatch.existingConfigMaps "db") . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "chart-wrapper.onlineWatchAppLabel" . }}
    {{- include "chart-wrapper.labels" . | nindent 4 }}
data:
  {{- $glob := "files/onlinewatch/internal-config/db/*" -}}
  {{- $internal := ((.Files.Glob $glob).AsConfig | fromYaml) | default dict -}}
  {{- $externalRaw := (($onlineWatch.internalConfig | default dict).dbExternal | default dict) -}}
  {{- $external := dict -}}
  {{- range $k, $v := $externalRaw }}
  {{- if kindIs "string" $v }}
  {{- $path := trimPrefix "./" ($v | replace "\\" "/") -}}
  {{- $_ := set $external $k (required (printf "missing file for ressuite.online-watch-be.internalConfig.dbExternal.%s: %s" $k $path) ($.Files.Get $path)) -}}
  {{- else }}
  {{- $_ := set $external $k $v -}}
  {{- end }}
  {{- end }}
  {{- mergeOverwrite $internal $external | toYaml | nindent 2 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ tpl (index $onlineWatch.existingConfigMaps "search") . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "chart-wrapper.onlineWatchAppLabel" . }}
    {{- include "chart-wrapper.labels" . | nindent 4 }}
data:
  {{ (.Files.Glob "files/onlinewatch/internal-config/search/*").AsConfig | nindent 2 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ tpl (index $onlineWatch.existingConfigMaps "templateExcel") . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "chart-wrapper.onlineWatchAppLabel" . }}
    {{- include "chart-wrapper.labels" . | nindent 4 }}
binaryData:
  {{ (.Files.Glob "files/onlinewatch/internal-config/templateExcel/*").AsSecrets | nindent 2 }}
{{- end }}
